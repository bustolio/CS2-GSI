package com.cs2gsi.nodes;

/**
 * Enum list for each tournament draft game state.
 * (Named GameState in the original C# library.)
 */
public enum DraftState {
    /** Undefined. */
    Undefined,

    /** The game is live. */
    Live
}
